# Gate Decisions — Defect Run 2026-05-31-bd382bcd

> Defect-run gates D0–D5 (base run contract). Not feature G0–G8 stages.

## Gate Decisions

| Gate | Decision | Decider | Timestamp | Rationale | Blocking | Follow-up |
|------|----------|---------|-----------|-----------|----------|-----------|
| D0 DEFECT SCOPE LOCK | PASS | Orchestrator | 2026-05-31T16:53:00-04:00 | Bug statement, expected/observed behavior, active roles (architect, frontend-developer), and ALLOW_FEATURE_PROPOSAL=false confirmed. AFFECTED_PATHS empty at intake → discover during triage. | No | - |
| D1 REPRODUCTION AND TRIAGE | PASS | Architect | 2026-05-31T17:05:00-04:00 | Live (dynamic) reproduction unavailable — needs a running OIDC IdP + backend + intermittent renewal-failure timing (env access not available; documented). Reproduced at root-cause level via static trace: pre-fix `renewSessionForExpiredToken` calls `signinSilent` once and surfaces any failure (incl. transient `idp_unreachable`) → both callers (`api.ts`, `ProtectedRoute.tsx`) `beginForcedReauth` → `/login?...&return_to=`. Encoded as a regression test (red on pre-fix code) plus a standalone logic simulation. | No | - |
| D2 ROOT CAUSE AND FIX PLAN | PASS | Architect | 2026-05-31T17:10:00-04:00 | Root cause: zero tolerance for transient silent-renewal failures, amplified by `automaticSilentRenew: false` (renewals run on the live request/navigation path). Smallest fix: bounded single retry for transient (`idp_unreachable`) failures in `renewSessionForExpiredToken`; terminal causes still fail fast. No feature tracking required for the defect fix; durable hardening recorded as follow-up. | No | - |
| D3 IMPLEMENTATION | PASS | Frontend Developer | 2026-05-31T17:25:00-04:00 | Implemented bounded transient retry in `sessionRenewal.ts`; added focused regression tests. Callers unchanged. | No | - |
| D4 VALIDATION | PASS WITH RECOMMENDATIONS | Frontend Developer | 2026-05-31T17:40:00-04:00 | Static validation green: TypeScript transpile check of both edited files = 0 errors; standalone control-flow simulation = 8/8 assertions pass. Runtime `vitest`/`lint`/`build` could NOT execute — pnpm cannot finish `node_modules` linking on the `/mnt/c` WSL mount (`ERR_PNPM_EACCES` on temp-dir rename), unrelated to the fix and beyond the one repair cycle. Vitest regression suite **escalated to CI / Linux-native install**. See `artifacts/test-results/d4-env-blocker.md`. | No | Run `pnpm exec vitest run sessionRenewal.test.ts`, `pnpm lint`, `pnpm build` in a Linux-native checkout/CI. |
| D5 REVIEW AND CLOSEOUT | PASS | Orchestrator | 2026-05-31T17:50:00-04:00 | Role reports written; README summarizes root cause/fix/validation/follow-ups; base run files complete. | No | - |
| FEATURE RECOMMENDATION (post-closeout, operator-requested) | RECOMMEND — PROMOTION PENDING | Product Manager | 2026-05-31T18:05:00-04:00 | At operator request, authored `feature-recommendation.md` for proactive silent token renewal (proposed F0037, follow-on to F0009/F0035). Recommendation only — no feature created; `REGISTRY.md` and `planning-mds/features/*` untouched. Awaiting operator promotion decision; if approved, this defect run stops cleanly and work moves to the formal `plan`/`feature` contract with a new feature-scoped run ID. | No | Operator to approve/defer/decline promotion (see Decision block in `feature-recommendation.md`). |
| PROMOTION DECISION | PROMOTION APPROVED — DEFECT RUN STOPPED | Operator | 2026-05-31T18:12:00-04:00 | Operator approved promotion of the proactive-renewal recommendation to formal feature work (F0037). _(Superseded by the revised row below.)_ | No | _(superseded)_ |
| PROMOTION DECISION (REVISED) | PROMOTION WITHDRAWN — DEFECT FIX SUFFICIENT | Operator | 2026-05-31T18:30:00-04:00 | Supersedes the row above. After reviewing F0035's PRD, the operator determined the reported bug is a defect *within* F0035's reactive silent renewal (no transient-failure tolerance), already resolved by this run's transient-retry fix. F0035 deliberately scoped renewal as reactive (≤5% forced-redirect target, not 0%); proactive pre-expiry renewal is optional hardening, not required to close the report. **F0037 is NOT pursued** as a separate feature. No feature/PRD/`REGISTRY.md` artifacts were created. Proactive renewal recorded as an optional backlog note only. | No | None required. Optional future: fold proactive renewal into an F0035 follow-on if ever desired. |

| ROOT CAUSE CORRECTION (post-runtime-evidence) | SUPERSEDES D2/D3/D4 | Architect | 2026-05-31T19:30:00-04:00 | The original D2 root cause (transient silent-renewal failure) was wrong — the bug reproduced live. Runtime logs (backend Serilog `/renewals`=200; DevTools CSP `frame-src` block on `http://localhost:9000`) proved the real cause: the authentik provider blueprint omits the `offline_access` scope mapping → no refresh token issued → `signinSilent()` uses a CSP-blocked iframe → forced reauth every 5-min access-token expiry. **Real fix:** add `offline_access` scope mapping in `docker/authentik/blueprints/nebula-dev.yaml`. The earlier `sessionRenewal.ts` transient-retry change was **reverted**. Separate 500 telemetry bug (Guid vs username `sub`) logged as follow-up. | No | Re-apply blueprint (restart authentik) + re-login; validate no redirect past 5 min and refresh_token present. Fix telemetry 500 separately. |

Decisions: `PASS`, `PASS WITH RECOMMENDATIONS`, `FAIL`, `SKIP`, `RECOMMEND — PROMOTION PENDING`, `PROMOTION APPROVED — DEFECT RUN STOPPED`, `PROMOTION WITHDRAWN — DEFECT FIX SUFFICIENT`, `SUPERSEDES D2/D3/D4`. Blocking values: `Yes` / `No`.

> Timestamps reflect the working sequence of this run; they are an audit aid, not a billing record.
