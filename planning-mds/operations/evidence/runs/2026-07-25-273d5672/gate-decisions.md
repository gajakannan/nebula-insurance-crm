# Gate Decisions — F0039-neuron-multi-thread-conversations run 2026-07-25-273d5672

> `feature` action (Feature Evidence Contract, scope `feature-completion`, policy `2026-07-11`).
> Gates G0–G8 per `agents/actions/spec/feature.yaml`. Severity profile: `standard` (coverage floor 80%).
> Run scope: **S0001–S0008**; S0009 gated-deferred by operator decision (2026-07-25).

## Gate Decisions

| Gate | Title | Decision | Decider | Timestamp | Rationale | Blocking | Follow-up |
|------|-------|----------|---------|-----------|-----------|----------|-----------|
| G0 | Architect assembly plan authoring and validation | PASS | Architect | 2026-07-25 | Existing `feature-assembly-plan.md` (authored at plan run `2026-07-21-6eeb172f`) **reconciled**, not overwritten. All four Step-0.5 checks pass; 4 additive plan↔as-built reconciliations recorded (D1 extend `NeuronRepository`; D2 async-first repository; D3 `mock_provider` joins the S0004 set; D4 vendor the 3 intent schemas). STATUS.md `Required Role Matrix` aligned to canonical validator role names (`Security` → `Security Reviewer`) and `DevOps` added as a scope-forced role. `validate-feature-evidence.py --stage G0` exit 0. | No | Two runtime dependencies (Postgres, local Phi vLLM) handed to G1. |
| G1 | Runtime preflight | PASS | DevOps | 2026-07-25 | Initial probe **failed** (`docker` absent from the WSL distro; nothing serving on :8000) and was recorded as `runtime-blocked` with **no code edits made while blocked**. After the operator enabled Docker Desktop WSL integration and started vLLM per the runbook, preflight re-ran green: `nebula-db` Up (healthy) + reachable from the Neuron venv (PostgreSQL 16.14), `nebula-authentik-server` Up (healthy), `nebula-api` Up (`/healthz` 200), local Phi serving `microsoft/Phi-4-mini-instruct` on vLLM 0.25.1 with **strict `json_schema` structured output verified**. Pre-change test baseline green (116 tests, 0 failures). | No | Compose `neuron` service still pins `NEURON_MODEL_PROVIDER=mock` / `NEURON_PERSISTENCE=memory`; updated and re-verified at the G2 deployability check. |
| G2 | Self-review, QE, and deployability | PASS | Quality Engineer | 2026-07-25 | Scope booleans reconciled against the 64 actual `changed_paths[]` first: all four flip true (`experience/**`, `neuron/**` runtime, compose+config, owner-scoping/injection/secret handling). **410 neuron tests pass** (baseline 116) incl. 27 real-Postgres and 13 live-Phi; **coverage 90%** vs the 80% floor; frontend 329/330 + build + theme guard green. Four security scan classes executed or waived: pip-audit clean, bandit 0 high (9 medium B608 individually verified false-positive — only constants/literals reach SQL, all user values parameterized), secrets via pattern fallback (gitleaks unavailable), DAST waived with complete waiver. DevOps updated the `neuron` compose service (postgres persistence, bounded pool, `depends_on: db healthy`, shadow-mode default) and verified migration idempotency against the live database. | No | **§30.4 rollout gate is RED (2 of 8) — direct routing NOT enabled; shipped default is shadow.** Carried to G4 as a product/architecture decision, not a code defect. Secrets scanned with a weaker fallback tool — Security to rule at G3. |
| G3 | Code and security review (parallel) — re-reviewed after the M1 fix | PASS WITH RECOMMENDATIONS | Code Reviewer + Security Reviewer | 2026-07-25 | **0 critical, 0 high** across both reviews (code: 4 medium/3 low; security: 2 medium/2 low). Code review confirms the vertical slice is complete and that rejection is *structural* — `target_head_card_id` is None on every failure path, so "no engine call" cannot be forgotten. Security independently re-verified the 9 bandit B608 findings as false positives (only constants/literals reach SQL) and confirmed the three properties that bound a tricked model: it is given no records/token/history, it cannot name a head (catalog resolves it), and it grants no authorization. Adversarial set 100% detect/redirect vs live Phi; fail-closed 1.000. Cross-user access returns 404 not 403, and injection vs off-topic redirects are byte-identical — both tested. | No | Before **direct** routing is enabled: run gitleaks in CI (S2 — secrets used a weaker fallback tool here) and pin `model_revision`/`image_digest` (S3). M1 (assistant-envelope persistence failure is swallowed) needs an explicit product answer. All are follow-ups, not merge blockers. |
| G4 | Approval | PASS | User (operator) | 2026-07-25 | `gate_policy.py --profile standard` computed **ACCEPTABLE** (critical 0, high 0; `requires_justification: false`, allowed `[approve, fix issues]`), so approval was enabled without a mitigation token. **Operator decision: approve, conditional on fixing M1 first.** M1 fixed and re-verified before approval was recorded: an assistant envelope that cannot be persisted is now marked with a `failed` status part ("This reply could not be saved to your conversation.") instead of silently looking saved; the reply content is still returned. 3 regression tests added — suite **413 passing**, coverage still **90%**, bandit still 0 high. The operator was also explicitly presented with, and accepted, shipping in **shadow mode** with direct routing withheld while the §30.4 gate is red. | No | Open follow-ups carried to closeout: 5 medium / 5 low (gitleaks in CI, pin model_revision/image_digest, resolver private-attr access, local imports, postgres.py fault-injection coverage, and the low-severity items). §30.4 accuracy gap is a follow-up run, not a defect. |
| G5 | Signoff | PASS | Product Manager | 2026-07-25 | All six `Required = Yes` roles recorded a passing verdict with reviewer, ISO date, and an evidence path under the run folder; 48 per-story rows (8 stories x 6 roles) written to STATUS.md Story Signoff Provenance. Two verdicts are `PASS WITH RECOMMENDATIONS` (Code Reviewer, Security Reviewer) and each of the five §15 closeout-passing conditions is satisfied explicitly in the ledger — non-blocking, severity stated (0 critical/0 high), owner+follow-up named, PM acceptance recorded at G4, and no blocking language. | No | Security signoff attaches two conditions **before direct routing** (gitleaks in CI; pin model_revision/image_digest) — not conditions on this merge. |
| G6 | Candidate evidence validation | PASS | Quality Engineer | 2026-07-25 | `feature-action-execution.md` records the gate-by-gate timeline. Candidate state confirmed: manifest `in-progress`, `gate_results` populated through `signoff`, **no `pm_closeout`, no `tracker_sync`, no `latest-run.json`** — closeout has not begun. `changed_paths[]` 64 entries; all four conditional booleans cross-checked against the §7 path classes at G2; the one non-required absent artifact (DAST) is recorded as a complete waiver in `security_scans`. `validate-trackers.py --feature F0039 --run-id …` **PASS (0 errors, 0 warnings)**. | No | Carried to closeout: red §30.4 rollout gate (shadow mode), 5 medium / 5 low accepted follow-ups, two pre-existing repo failures outside F0039 scope, and two `resume-brief.py` framework defects. |
| G7 | Architect knowledge-graph reconciliation | PASS | Architect | 2026-07-25 | Six binding shards authored in `kg-source/bindings/node_bindings.yaml` for the as-built neuron/ + experience/ surfaces (glob-based); the pre-existing `capability:neuron-companion` glob already covered the files, so the new shards narrow rather than duplicate. `capability:neuron-intent-adjudicator` left intentionally unbound (S0009 gated, no code). **No canonical nodes introduced** — the plan-run shards already matched the as-built surface. `knowledge-graph/*.yaml` never hand-edited: compile.py regenerated the projection trio + tracker regions; `validate.py --regenerate-symbols --check-symbols --regenerate-decisions --check-decisions` **exit 0**; `--check-drift` **exit 0**. `--write-coverage-report` deliberately NOT run here (path-sensitive; deferred to G8 after the archive move). | No | Three drift warnings traced to this run's S0007 rename (`_route_in_scope` → `_route_head`): the symbol regenerator left stale inbound `callers` edges. Cosmetic, non-blocking, integrity checks pass — carried as a follow-up so the next run does not inherit an unexplained warning. |
| G8 | PM closeout | PASS | Product Manager | 2026-07-25 | **Feature closed as Done and archived** to `planning-mds/features/archive/F0039-neuron-multi-thread-conversations`. The gated ninth story was **promoted to a new feature F0041** (Neuron Contextual Intent Adjudicator) rather than archived unbuilt inside F0039 — operator decision; archiving a feature containing work nobody did would misrepresent what shipped. Trackers synced (STATUS Done, REGISTRY F0039→Done/archive + F0041 added, ROADMAP F0039→Completed / F0041→Later, kg-source shards repointed). `compile.py` regenerated all projections — `knowledge-graph/*.yaml` never hand-edited. `patch-prior-manifest.py` exit 0 (no prior approved manifests); `latest-run.json` written **after** it; manifest → **approved**. `--write-coverage-report` run **after** the move (path-sensitive), then `--check-drift` **PASS**. Telemetry captured (352 turns, 99.4% cached). | No | 9 deferred follow-ups recorded in `pm-closeout.md` with owners. Direct routing remains **withheld** pending a green §30.4 report, gitleaks in CI, and pinned model provenance. |

Decisions: `PASS`, `PASS WITH RECOMMENDATIONS`, `FAIL`, `SKIP`, `PENDING`, `NOT STARTED`. Blocking values: `Yes` / `No`.

## Notes

- **Concurrent-run check (pre-G0):** the only other F0039 manifest is the plan run `2026-07-21-6eeb172f` at
  status `draft`. It carries `run_scope: base-run-only` while this run is `feature-completion`, so
  `init-run.py`'s scope-aware active-run scan does not treat it as a competing run. No external reconciliation
  was required and the prior manifest was left untouched.
- **Scope booleans:** `runtime_bearing`, `frontend_in_scope`, `deployment_config_changed` set true at G0;
  `security_sensitive_scope` is held false until G2 because the `security_scans{}` block is validated at every
  stage once true, and the four scan classes only run at G2. Rationale recorded in
  `g0-assembly-plan-validation.md`.
