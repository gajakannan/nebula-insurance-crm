# PM Closeout — F0039 run `2026-07-25-273d5672`

**Gate:** G8 · **Role:** Product Manager · **Date:** 2026-07-25
**Final feature state: Done** · **Archived to** `planning-mds/features/archive/F0039-neuron-multi-thread-conversations`

## Final Story Status

| Story | Status |
|-------|--------|
| F0039-S0001 Durable conversation store | **Done** |
| F0039-S0002 Owner-scoped thread & history API | **Done** |
| F0039-S0003 Conversation-first panel | **Done** |
| F0039-S0004 Structured provider & Phi profile | **Done** |
| F0039-S0005 Catalog, prompts & resolution contract | **Done** |
| F0039-S0006 Preflight & direct resolver | **Done** |
| F0039-S0007 Dispatcher & provenance integration | **Done** |
| F0039-S0008 Evaluation, shadow mode & rollout | **Done** |
| ~~F0039-S0009~~ Contextual adjudicator | **Moved → F0041-S0001** (gated, not built) |

## What shipped

F0039 delivers durable Neuron conversations and a local Phi intent-resolution layer:
S0001–S0008, 8 of the feature's 9 planned stories. Suite grew 116 → 413 tests; coverage 90%.

## Archive Decision

F0039 is **Done** and archived to `planning-mds/features/archive/F0039-neuron-multi-thread-conversations`.
The archived folder equals what shipped: 8 delivered stories, with the gated ninth promoted out rather than
left inside as unbuilt work.

## The ninth story: promoted, not quietly dropped

`F0039-S0009` (contextual adjudicator) was **gated from the outset** — its entry condition
is that F0039's own direct-routing and context gates pass, which are the gates *this run
produces*. It could not honestly be built here.

At the operator's direction it is **promoted to a new feature, F0041 — Neuron Contextual
Intent Adjudicator (gated follow-on)**, rather than archived inside F0039 as an unbuilt
story. Archiving a feature that contains work nobody did would misrepresent what shipped;
promoting it keeps the archived feature equal to reality and leaves the deferred work
visible in the backlog with its entry conditions written down.

The story file moved to `planning-mds/features/F0041-neuron-contextual-adjudicator/`, its
KG shard was split out, and the behaviour it will eventually replace is already implemented
and tested: an `adjudicate` decision degrades to a bounded clarify.

## Rollout state at closeout — read this before enabling direct routing

The §30.4 evaluation is **RED on two of eight gates**: `authorization_bypasses` 1 (target 0)
and `domain_accuracy` 0.933 (target 0.95). **All security gates are green** — injection
detect 1.000, redirect precision 1.000, fail-closed 1.000.

The shipped default is therefore `NEURON_INTENT_MODE=shadow`, pinned by a test so it cannot
drift to `direct`. Shadow is observationally identical to the F0038 deterministic guard, so
this is safe to deploy and gathers the comparison data the gate needs. **Enabling direct
routing is a separate decision requiring a green evaluation report** — plus gitleaks in CI
and pinned `model_revision`/`image_digest` per the Security signoff.

This is the specified behaviour, not a waiver: the gate worked and withheld a rollout.

## Tracker Updates

| Tracker | Change |
|---------|--------|
| `STATUS.md` | Overall Status → **Done**; S0001–S0008 Done; S0009 row marked Moved → F0041 |
| `REGISTRY.md` | F0039 → Done, path → `archive/…`; **F0041 added** (Planned) |
| `ROADMAP.md` | regenerated from shards via `compile.py` |
| `kg-source/features/F0039.yaml` | `status: done`, `path:` → archived, S0009 block removed |
| `kg-source/features/F0041.yaml` | **new shard** for the promoted story |

KG projections were regenerated by `compile.py` — never hand-edited.

## Deferred Follow-ups

Accepted at G4/G5; none blocking.

| # | Item | Owner | When |
|---|------|-------|------|
| S2 | Run `gitleaks` in CI (this run used a pattern-scan fallback) | DevOps/Security | Before a deployed environment |
| S3 | Pin `model_revision` / `image_digest` | DevOps | Before direct routing |
| M2 | Expose read-only `catalog`/`prompt` on `IntentResolver` | AI Engineer | Follow-up |
| M3 | Hoist function-local imports in `evaluation.py` | AI Engineer | Follow-up |
| M4 | Fault-injection coverage for `postgres.py` error branches | QE | Follow-up |
| L1 | Use catalog `description` in confirmation copy, not action ids | Backend | Follow-up |
| L2 | Single-source the action-count policy (catalog vs schema) | Architect | Follow-up |
| — | §30.4 accuracy gap (prompt/dataset work) | AI Engineer | Before direct routing |
| — | Stale `callers` edges after the S0007 rename (regenerator gap) | Architect | Next KG pass |

**M1 was not deferred** — the operator required it fixed before approving, and it was
(assistant envelope now marked `failed` when unsaved; 3 regression tests).

## Outside F0039 scope, deliberately untouched

Two pre-existing repo failures were left alone rather than widening scope: a frontend test
(`CreateSubmissionPage.integration.test.tsx`, proven pre-existing by stashing this run's
changes) and an unused-import lint error in an untouched F0037 e2e spec. Also recorded: two
`resume-brief.py` defects in the framework repo (wrong "next gate", unresolvable default
workstate path).

## Validator Results

| Validator | Result |
|-----------|--------|
| `compile.py` (post-move) | wrote all projections + tracker regions |
| `validate.py --write-coverage-report` (after move) | **PASS** |
| `validate.py --check-drift` (post-move) | **PASS** |
| `patch-prior-manifest.py` | exit 0 — no prior approved manifests |
| `validate-feature-evidence.py --stage closeout` | **PASS** |
| `validate-trackers.py --feature F0039 --run-id …` | **PASS** |
| `capture-run-telemetry.py` | token-usage.json written (352 turns, 99.4% cached) |

## Validator-defect note

No validator defect blocked closeout. Several gates required evidence-shape corrections
(missing headings, per-story ledger mirroring, a trailing period breaking an artifact path)
— all genuine gaps in my artifacts, corrected and re-run, not bypassed. No
`--evidence-effective-date` override was used at any point.

## Recommendation Acceptances

All items above are accepted as post-merge follow-ups by the operator's G4 approval, which
was granted conditional on the M1 fix that was completed and re-verified beforehand.

The §15 PM Acceptance Lines below cover every `WITH RECOMMENDATIONS` signoff row:

- Accepted: F0039-S0001-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0001-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0002-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0002-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0003-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0003-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0004-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0004-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0005-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0005-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0006-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0006-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0007-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0007-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
- Accepted: F0039-S0008-Code Reviewer — code review recommendations are non-blocking follow-ups (0 critical, 0 high); M1 was fixed and re-verified before approval; remaining items owned per the closeout table.
- Accepted: F0039-S0008-Security Reviewer — security verdict approves shadow-mode rollout (0 critical, 0 high; all §30.4 security gates green); gitleaks in CI and pinned model provenance are required before direct routing, not before this merge.
