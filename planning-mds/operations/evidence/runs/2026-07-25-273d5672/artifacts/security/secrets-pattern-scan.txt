# Secrets scan — pattern-based fallback (gitleaks unavailable on this host)
# Scanned: all files changed by F0039 (see artifacts/diffs/changed-files.txt)
# Patterns: API keys, bearer tokens, private keys, passwords, connection strings with credentials

32:    "postgresql://postgres:postgres@127.0.0.1:5433/nebula",

Files with candidate matches: 1
