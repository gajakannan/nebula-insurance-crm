{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nebula.local/schemas/admin-configuration-publish-request.schema.json",
  "title": "AdminConfigurationPublishRequest",
  "description": "Publish or rollback confirmation payload for admin configuration.",
  "type": "object",
  "required": ["reason"],
  "properties": {
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500
    },
    "targetPublishedVersion": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Required for rollback requests; omitted for draft publish."
    }
  },
  "additionalProperties": false
}
