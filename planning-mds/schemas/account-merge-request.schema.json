{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nebula.local/schemas/account-merge-request.schema.json",
  "title": "AccountMergeRequest",
  "description": "Merge the path-parameter account (source) into survivorAccountId. Source must be Active or Inactive; survivor must be Active; no self-merge. See ADR-017.",
  "type": "object",
  "required": ["survivorAccountId"],
  "properties": {
    "survivorAccountId": { "type": "string", "format": "uuid" },
    "notes": { "type": ["string", "null"], "maxLength": 500 }
  },
  "additionalProperties": false
}
