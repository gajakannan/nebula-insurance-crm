{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nebula.local/schemas/submission-assignment-request.schema.json",
  "title": "SubmissionAssignmentRequest",
  "description": "Validation rules for assigning or reassigning submission ownership (F0006-S0006).",
  "type": "object",
  "required": [
    "assignedToUserId"
  ],
  "properties": {
    "assignedToUserId": {
      "type": "string",
      "format": "uuid",
      "description": "Target user to assign to. Must be a valid, active internal user. When submission is in ReadyForUWReview, target must have Underwriter role."
    }
  },
  "additionalProperties": false
}
