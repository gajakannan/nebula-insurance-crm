{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nebula.local/schemas/policy-import-request.schema.json",
  "title": "PolicyImportRequest",
  "description": "Import-lite CSV ingest contract (F0018-S0002). One request carries a batch of imported policies that land directly in Issued state with ImportSource='csv-import'. Per-row validation applies policy-create rules.",
  "type": "object",
  "required": ["rows"],
  "properties": {
    "importBatchReference": { "type": ["string", "null"], "maxLength": 100 },
    "rows": {
      "type": "array",
      "minItems": 1,
      "maxItems": 500,
      "items": { "$ref": "https://nebula.local/schemas/policy-create-request.schema.json" }
    }
  },
  "additionalProperties": false
}
