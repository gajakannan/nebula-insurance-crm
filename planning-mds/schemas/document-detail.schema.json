{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://nebula.local/schemas/document-detail.schema.json",
  "title": "DocumentDetail",
  "description": "GET /documents/{id} response. Wraps the canonical sidecar JSON and adds previewUrls for renderable versions.",
  "type": "object",
  "required": ["sidecar", "previewUrls"],
  "additionalProperties": false,
  "properties": {
    "sidecar": { "$ref": "document-sidecar.schema.json" },
    "previewUrls": {
      "type": "array",
      "description": "Aligned by index with sidecar.versions[]. null where the version is non-renderable or non-available.",
      "items": { "type": ["string", "null"], "format": "uri" }
    }
  }
}
