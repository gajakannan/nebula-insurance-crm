# Placeholder model configuration for Nebula.
# Keep credentials in environment variables.
providers:
  anthropic:
    model: claude-3-5-sonnet
    api_key_env: ANTHROPIC_API_KEY
  ollama:
    base_url: http://localhost:11434
    model: llama3.1
