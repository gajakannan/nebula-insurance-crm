# Broker activity specialist head (F0040). Read-only; the forwarded user token is
# authorized by the engine's scoped internal Broker timeline projection.
card_id: crm.broker_activity.head
card_version: 1.1.0
kind: specialist_head
name: Broker activity
description: Shows the newest twenty authorization-scoped Broker timeline events.
active: true
public: false
auth_mode: user_token
accepted_output_modes: [app, status]
capabilities:
  - skill_id: broker_activity_read
    description: List recent authorization-scoped Broker activity.
tools:
  - engine.timeline.list_broker_activity
components:
  - broker_activity.recent_list
