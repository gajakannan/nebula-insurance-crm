version: 1
metadata:
  name: "nebula: default authentication flow"
  labels:
    app: "nebula"
    component: "authentik"
    purpose: "placeholder"

# No entries — authentik's built-in default blueprints already create
# all required authentication and authorization flows (default/, system/).
# This file is kept as a placeholder so the blueprint instance remains registered.
entries: []
